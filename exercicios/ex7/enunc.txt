

Você está construindo um sistema de registro de eventos. Cada evento será representado por uma tupla que deve conter exatamente três elementos:

O Nome do Evento (uma string).

O Status do Evento (um boolean, onde true significa "Ativo" e false significa "Cancelado").

A Capacidade Máxima de participantes (um number).


eventName(string),eventStatus(boolean),maxPartCap(number)

*event()


Tarefas

Definir a Tupla: Crie um type alias chamado RegistroEvento que defina a estrutura da tupla (string, boolean, number).

Criar Tuplas: Declare três variáveis usando o tipo RegistroEvento, inicializando-as com dados válidos.

Acessar e Imprimir: Crie uma função chamada exibirDetalhes que aceite uma tupla do tipo RegistroEvento como argumento e imprima o nome do evento,
seu status (imprimindo "Ativo" ou "Cancelado" em vez de true/false), e sua capacidade.

Teste de Tipo Inválido (Conceitual): Descreva, em um comentário no seu código (ou em texto à parte), o que aconteceria se você tentasse atribuir
um array com um tipo incorreto ou número de elementos diferente à sua variável RegistroEvento (por exemplo, [123, true, "Palestra"]).



*************************************************||**************************************************************



O exercício a seguir ajudará você a entender como usar enums em TypeScript para definir um conjunto de constantes nomeadas, tornando seu código mais legível e menos propenso a erros de digitação.

📝 O Cenário
Você está desenvolvendo um Sistema de Gerenciamento de Tarefas simples. Cada tarefa pode ter um de três estados possíveis:

Pendente (Pending)

Em Andamento (InProgress)

Concluída (Completed)

🎯 O Objetivo
Crie um enum para representar os possíveis estados de uma tarefa.

Crie uma interface Tarefa que utilize esse enum.

	Implemente uma função que aceite uma lista de tarefas e um estado, e retorne apenas as tarefas que correspondem a esse estado.

Crie uma função para marcar uma tarefa como concluída (mudar o estado para Completed).













// 1. Defina o type alias da tupla RegistroEvento aqui.
// type RegistroEvento = ???;

// 2. Crie as três tuplas aqui.
// const eventoConferencia: RegistroEvento = ???;
// const eventoWorkshop: RegistroEvento = ???;
// const eventoCancelado: RegistroEvento = ???;

// 3. Crie a função exibirDetalhes aqui.
// function exibirDetalhes(evento: RegistroEvento): void {
//   // Lógica para desestruturar e imprimir
// }

// Chame a função para testar:
// exibirDetalhes(eventoConferencia);
// exibirDetalhes(eventoWorkshop);
// exibirDetalhes(eventoCancelado);

// 4. Descreva o teste de tipo inválido aqui:
/* O que aconteceria se tentássemos: 
const eventoInvalido: RegistroEvento = [123, true, "Palestra"];
*/
